# Quickstart

## Run locally

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.

## Production check

```bash
npm run build
```

## Main folders

- `app/` for routes
- `components/` for shared UI
- `lib/` for shared config/content
- `styles/` for global styles
